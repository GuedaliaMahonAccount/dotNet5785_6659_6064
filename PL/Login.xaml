<Window x:Class="PL.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Login" Height="800" Width="1200"
        Loaded="Window_Loaded" Icon="Assets/icon.png"
        Closed="Window_Closed"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Closing="Window_Closing">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="288*"/>
            <RowDefinition Height="35*"/>
            <RowDefinition Height="35*"/>
            <RowDefinition Height="35*"/>
            <RowDefinition Height="5*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="505*"/>
            <ColumnDefinition Width="312*"/>
            <ColumnDefinition Width="383*"/>
        </Grid.ColumnDefinitions>

        <Grid.Background>
            <ImageBrush ImageSource="Assets\loginitem.jpg" Stretch="UniformToFill"/>
        </Grid.Background>

        <!-- Admin and User Text -->
        <StackPanel Orientation="Vertical" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="10" Grid.Column="2" Grid.Row="0">
            <TextBox Text="Dani Levy , aaaaAAAA1111" 
                     FontSize="12" FontWeight="Bold" Foreground="Gray" 
                     Background="Transparent" BorderThickness="0" IsReadOnly="True"/>
            <TextBox Text="Eli Amar , bbbbBBBB2222" 
                     FontSize="12" FontWeight="Bold" Foreground="Gray" 
                     Background="Transparent" BorderThickness="0" IsReadOnly="True"/>
        </StackPanel>
        

        <!-- UserName TextBox -->
        <TextBox Name="NameTextBox" HorizontalAlignment="Stretch" VerticalAlignment="Top" TextWrapping="Wrap" Text="Enter Name" 
                 FontSize="30" FontWeight="Bold" Foreground="Black"
                 GotFocus="TextBox_GotFocus" LostFocus="TextBox_LostFocus" Grid.Column="1" Grid.Row="1" Margin="6,0"
                 Height="50"/>

        <!-- Password PasswordBox -->
        <PasswordBox Name="PasswordTextBox" HorizontalAlignment="Stretch" VerticalAlignment="Top" 
                     FontSize="30" FontWeight="Bold" Foreground="Black"
                     GotFocus="PasswordBox_GotFocus" LostFocus="PasswordBox_LostFocus" Grid.Column="1" Grid.Row="2" Margin="6,0"
                     Height="50"/>

        <!-- Login Button -->
        <Button Content="Login" HorizontalAlignment="Stretch" VerticalAlignment="Center" Height="50"
                FontSize="30" FontWeight="Bold" Foreground="White" Background="Green"
                Click="LoginButton_Click" Grid.Column="1" Grid.Row="3" Margin="6,0"/>
    </Grid>
</Window>
